1. Use `grep` to find all DTO-based bulk update methods in `src/main/java/com/divudi/bean/pharmacy/PharmacyController.java`.
2. Use `replace_with_git_merge_diff` to refactor the discovered bulk update methods in `src/main/java/com/divudi/bean/pharmacy/PharmacyController.java` to implement the batch fetching logic using a parameterized `IN` clause.
3. Use the `read_file` tool to review the modified sections of `src/main/java/com/divudi/bean/pharmacy/PharmacyController.java` to ensure the syntax and logic are correct.
4. Run `mvn clean test` to ensure there are no regressions.
5. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
