I will fix the critical SQL injection vulnerabilities in `src/main/java/com/divudi/bean/report/LabReportSearchByInstitutionController.java`, `src/main/java/com/divudi/bean/report/InstitutionLabSumeryController.java`, and `src/main/java/com/divudi/bean/report/LabReportSearchByDepartmentController.java`. The `txtSearch` parameter is being directly concatenated into the JPQL query string, allowing for SQL injection.

I will use `replace_with_git_merge_diff` to modify the `searchAll()` method in all three controllers. I will also change `createPatientInvestigaationList()` method in `LabReportSearchByInstitutionController.java` and `InstitutionLabSumeryController.java`, and `getPatientInvestigations()` method in `LabReportSearchByDepartmentController.java` to use parameterized queries and `upper()` in the query string.
